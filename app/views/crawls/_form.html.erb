<%= form_for(@crawl) do |f| %>
  <% if @crawl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawl.errors.count, "error") %> prohibited this crawl from being saved:</h2>

      <ul>
      <% @crawl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :stops %><br>
    <%= f.number_field :stops %>
  </div>
  
   <%= f.hidden_field :user_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
