<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @crawl.title %>
</p>

<p>
  <strong>Stops:</strong>
  <%= @crawl.stops %>
</p>

<p>
  <strong>User:</strong>
  <%= @crawl.user %>
</p>

<%= link_to 'Edit', edit_crawl_path(@crawl) %> |
<%= link_to 'Back', crawls_path %>
